import { useEffect, useState } from "react";

/**
 * Hook to determine when the component has mounted
 */
export function useHasMounted() {
  const [hasMounted, setHasMounted] = useState(false);

  useEffect(() => {
    setHasMounted(true);
  }, []);

  return hasMounted;
}
